# Threaded cap (7Y)

Round screw-in cap with external thread and 4 internal pins, modelled from photos.
Parametric: edit `threaded_cap.scad` (cap_d, thread_major, thread_pitch, thread_len, pins).

Print: flat head down, no supports. PETG/ASA, 3 walls, 0.2 mm layer (0.6 nozzle OK).
